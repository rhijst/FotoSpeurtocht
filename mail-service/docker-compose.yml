services:
  mail-service:
    build: .
    container_name: mail-service
    env_file:
      - .env
    environment:
      - REGISTER_SERVICE_URL=http://host.docker.internal:3001
    ports:
      - "3004:3004"
    volumes:
      - .:/app
      - /app/node_modules